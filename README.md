# agampandey.github.io
portfolio

## Run locally

Serve the site from the repo root, then open the URL in a browser:

```bash
python3 -m http.server 8000
# open http://localhost:8000/
```

Stop the server when done:

```bash
lsof -ti:8000 | xargs kill
```
